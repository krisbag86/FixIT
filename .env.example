DATABASE_URL="postgresql://user:password@localhost:5432/fixit"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me"
EMAIL_FROM="IT Helpdesk <it@bagietka.pl>"
APP_URL="http://localhost:3000"

# --- SMTP (email wysylanie) ---
SMTP_HOST=""
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER=""
SMTP_PASSWORD=""

# --- Data provider ---
# W produkcji (Railway) automatycznie uzywa Prisma gdy DATABASE_URL jest ustawiony.
# Na lokalnie: domyslnie JSON store. Mozna wymusic:
# FIXIT_DATA_PROVIDER=json   # zawsze JSON store
# FIXIT_DATA_PROVIDER=prisma # zawsze Prisma (wymaga dzialajacej bazy)

# --- Pierwszy deploy na Railway ---
# FIXIT_RUN_SEED=true  # odkomentuj przy pierwszym deployu, potem zakomentuj
